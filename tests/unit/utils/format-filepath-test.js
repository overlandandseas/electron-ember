import formatFilepath from 'electron-playground/utils/format-filepath';
import { module, test } from 'qunit';

module('Unit | Utility | format filepath');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatFilepath();
  assert.ok(result);
});
